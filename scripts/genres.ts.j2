export const genres = (
  [
{% for id, name in entities -%}
    [{{ id | tojson }}, {{ name | tojson }}]{% if not loop.last %},{% endif %}
{% endfor -%}
  ] as const
).map(([id, name]) => ({ id: id.toString(), name }))